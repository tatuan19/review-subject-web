<%= form_with(model: subject, local: true) do |form| %>
  <% if subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subject.errors.count, "error") %> prohibited this subject from being saved:</h2>

      <ul>
        <% subject.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :subject_code %>
    <%= form.number_field :subject_code %>
  </div>

  <div class="field">
    <%= form.label :subject_name %>
    <%= form.text_field :subject_name %>
  </div>

  <div class="field">
    <%= form.label :number_of_credits %>
    <%= form.number_field :number_of_credits %>
  </div>

  <div class="field">
    <%= form.label :Description %>
    <%= form.text_field :Description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
